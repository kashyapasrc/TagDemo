name "Deploy preprod build"

on:
  push:
    tags:
      - 'pre_prod_*' # -> Pre Production release
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name : Run a one -line script
      run : echo PreProd build deployed
